import styled from "styled-components";

const S = {};

// ============== Log in form wrapper ================

S.LogInFormWrapper = styled.div`
  grid-area: form;
`;
// ============== Oauth wrapper ================
S.OauthBtnsWrapper = styled.div`
  grid-area: authBtns;
`;

export default S;
